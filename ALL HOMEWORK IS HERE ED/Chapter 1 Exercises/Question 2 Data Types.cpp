#include <iostream> //Pre-processor directive to include the iostream file for inputs and outputs
#include <string> // Used when variable has a string
using namespace std; //Declares the use of standard namespace

int main() {//The main function
    int firstNumber = 1; // Stores Integer (whole number)
    bool iCanCode = true; // Stores Boolean (True/False or Yes/No)
    char hopefulGrade = 'a'; //Used for storing single characters
    double myDecimal = 1.0; //Stores floating point numbers
    string minimalSentance = "y"; //Stores a whole text
    int keyMash = 13213123; // Integer (whole number)
    float mysteryDataType = 5.6f; //Used to store a real number

    return 0; //return statement 
}